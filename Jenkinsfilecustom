pipeline {
    agent any

    stages {
        
        stage('Maven - Build') {
            steps {
               
               bat 'mvn clean install'
               //maven clean install
            }
        }
    }
}
